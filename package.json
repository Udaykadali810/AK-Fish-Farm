{
    "name": "ak-fish-farms",
    "version": "2.0.0",
    "private": true,
    "description": "AK Fish Farms — Neon PostgreSQL + Vercel Serverless",
    "scripts": {
        "build": "echo 'Static site — no build needed'",
        "dev": "vercel dev",
        "start": "vercel dev"
    },
    "dependencies": {
        "@vercel/postgres": "^0.10.0",
        "bcryptjs": "^2.4.3",
        "pg": "^8.11.3"
    },
    "devDependencies": {
        "vercel": "latest"
    },
    "engines": {
        "node": ">=18.x"
    }
}